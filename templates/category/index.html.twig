{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!
{% endblock %}

{% block body %}
	<a href="{{ path('category_new')}}">Crée une category</a>
	<ul>
		{% for category in categorys %}
            <a href="{{ path("category_show", { CategoryName: category.name }) }}">{{category.name}}</a>
        {% endfor %}
	</ul>
{% endblock %}
